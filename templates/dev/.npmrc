# 使用 npm 官方源，避免第三方镜像与锁文件的完整性记录不一致。
registry=https://registry.npmjs.org
# pup
PUPPETER_DOWNLOAD_BASE_URL=https://npmmirror.com/mirrors/chrome-for-testing
# 不生成lock
package-lock=false
# 改为 npm 依赖安装方式
node-linker=hoisted
