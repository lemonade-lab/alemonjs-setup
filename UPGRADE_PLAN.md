# alemonjs-setup 升级计划

## 计划目标

将当前的 Go + React 模板升级为面向新手用户的 AlemonJS 图形化引导工具。升级以“先可用、后完善”为原则：每个阶段均应能独立运行、验证并交付。

## 当前基线与统一约定

当前仓库仍是模板状态：后端使用 Gin，仅提供 `/healthz` 与 `/api/ping`；前端使用 Vite + React 19。项目目标中的技术方向则为 Go 标准库与 React 18 CDN。

在开始功能开发前，必须完成以下技术决策，并在第一阶段一次性统一，避免后续重复迁移：

| 项目 | 升级约定 |
| --- | --- |
| 后端 | 迁移至 Go `net/http`，移除 Gin 依赖。 |
| 前端 | 使用仓库现有的 React + Vite 开发体验；生产构建产物由 Go `embed` 嵌入单文件。 |
| 运行模式 | 默认启动 Web 引导界面；同时保留面向自动化的 CLI 子命令。 |
| 系统调用 | 所有外部命令集中封装，必须有超时、日志、取消与中文错误转换能力。 |
| 安全边界 | 仅执行用户确认后的安装、创建、部署、发布操作；展示将执行的命令及影响范围。 |

## 里程碑

### M0：项目骨架收敛

**目标：** 从通用模板切换为可扩展的安装引导工具骨架。

- 建立 `internal/web`、`internal/service`、`internal/system`、`web`（嵌入式前端）与 `cmd`/CLI 的职责边界。
- 实现统一配置、版本信息、结构化日志和健康检查。
- 建立前端页面框架：欢迎页、目标选择页、任务详情页、结果页。
- 定义统一 API 返回格式、任务状态与错误码。

**验收：** 单个二进制启动后可访问 Web 界面；健康检查、版本查询与前端静态资源均正常。

### M1：环境检测与修复引导

**目标：** 用户可以看懂自己的电脑是否满足某个目的的运行条件。

- 支持按操作系统检测 Node.js、npm/yarn、Git、Docker、VS Code 与 npm 登录状态。
- 按“进行开发、安装桌面版、安装 Web 版、发布插件”分别生成环境检查清单。
- 将原始命令错误映射为中文问题、原因与解决建议。
- 对可安全自动完成的安装，提供确认后执行的一键修复；其余情况提供图文指引和官方下载入口。

**验收：** 在缺少任一依赖时，界面能指出缺失项、影响的目标与可执行的下一步；不暴露难以理解的原始报错作为主要信息。

### M2：开发项目创建流程

**目标：** 新手能在五步内创建并打开第一个 AlemonJS 项目。

- 收集项目名称、存放位置和模板选择。
- 采用“检查环境 → 选择项目方案 → 确认创建计划 → 创建项目 → 是否启动”的五步开发者流程；具体字段与模板能力补丁见 [`DEVELOPER_FLOW.md`](./DEVELOPER_FLOW.md)。
- 预检查目录权限与名称合法性，避免覆盖已有项目。
- 调用官方项目创建流程，展示实时日志与进度。
- 创建成功后可一键在 VS Code 中打开，并展示开发、预览与构建的下一步说明。

**验收：** 从目标选择到 VS Code 打开项目不超过五个主步骤；失败后用户能定位到失败阶段并重试。

### M3：桌面版与 Web 版安装流程

**目标：** 将“使用应用”与“自部署应用”做成独立、明确的引导路径。

- 桌面版：识别系统与架构，获取对应安装包，展示下载与安装状态，并在完成后打开应用。
- Web 版：检测 Docker 和端口占用，拉取/下载所需资源，生成最小默认配置，启动服务并打开访问地址。
- 保存可恢复的任务记录，支持查看运行状态、停止服务和重新打开访问地址。

**验收：** 两条路径均能显示明确的前置条件、执行进度和最终结果；Web 版能从界面确认服务健康状态。

### M4：应用构建流程

**目标：** 支持 npm 构建和遵循 AlemonJS 发布分支标准的 Git 化构建。

- npm 构建：检查 Node.js 与 npm，构建可分发产物。
- Git 化构建：检查 Node.js、Git、Yarn、jq；基于 `main`、`release` 分支和版本标签完成标准发布整理。
- 用户最终确认后执行构建，持续输出可读日志。

**验收：** 构建前能阻止常见错误；任何写入或推送操作必须经过最终确认，且结果可追溯。

### M5：CLI、扩展与交付质量

**目标：** 形成可自动化、可分发、可扩展的正式版本。

- 增加与 Web 能力对应的 CLI 子命令和非交互参数，支持 CI/CD。
- 定义 Agent 扩展接口：任务输入、进度事件、结果、权限确认与 Webhook 回调；不内置 Agent 实现。
- 补齐 Windows、macOS、Linux 的构建、签名/分发说明与版本发布流程。
- 补齐单元测试、关键流程集成测试、错误文案校验与跨平台手工验证清单。

**验收：** 同一核心流程可从 Web 或 CLI 调用；可产出三平台二进制；核心场景均有自动化测试或可重复的验收脚本。

## 实施优先级

| 优先级 | 范围 | 原因 |
| --- | --- | --- |
| P0 | M0、M1、M2 | 直接决定小白能否完成首次开发体验。 |
| P1 | M3 | 覆盖应用使用与部署需求。 |
| P2 | M4 | 覆盖创作者发布需求，依赖 npm 账号与发布策略。 |
| P3 | M5 | 提升自动化、扩展性与跨平台交付成熟度。 |

## 全程验收原则

- 用户主流程不超过 5 个操作步骤。
- 任何执行外部命令的操作均可见进度、可取消，并保留可复制的诊断日志。
- 任何有副作用的操作必须在执行前清晰告知影响，并获得用户确认。
- 错误信息优先呈现中文结论、影响与建议，技术原始日志放在“查看详情”。
- 每个里程碑完成后更新使用文档和验收记录，再进入下一阶段。

## 暂不纳入本轮计划

- 自研或内置 AI Agent。
- 代码编辑器、IDE 替代能力。
- 复杂的自定义项目模板与配置编排。
