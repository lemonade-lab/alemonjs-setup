#!/bin/bash
# Deprecated: this project is a Go application. Do not create a release branch
# or replace the repository contents with build output.
echo "此脚本已停用。请使用：albs --cwd \"$(pwd)\" git publish --yes" >&2
exit 2
